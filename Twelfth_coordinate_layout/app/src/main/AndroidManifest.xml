<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mina.twelfth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CoordinatorOneActivity" />
        <activity android:name=".CoordinatorTwoActivity" />
        <activity android:name=".MovieDetailActivity" />
        <activity android:name=".FlexboxOneActivity" />
        <activity android:name=".FlexboxTwoActivity" />
        <activity android:name=".FlexboxThreeActivity"></activity>
        <activity android:name=".ConstraintActivity"></activity>
    </application>

</manifest>